# 通用配置文件 - 主文件

# 端口号，用环境配置文件中的
#server:
#  port: 8080


# 应用名
spring:
  application:
    name: spring-ai-mcp-client
  profiles:
    active: local # 指定生效的环境配置


# 日志级别
logging:
  level:
    root: info

# 网络搜索引擎
websearch:
  searxng:
    url: http://localhost:6080/search
    counts: 10