# 端口号
server:
  port: 8080

spring:
  ai:
    openai:
      base-url: https://dashscope.aliyuncs.com
      api-key: your-sk
      chat:
        options:
          model: qwen3.6-plus
        completions-path: /compatible-mode/v1/chat/completions
